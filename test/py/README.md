# Python tests

Install dependencies before running the Python tests (e.g. in CI or Docker):

```bash
pip install -r test/py/requirements.txt
```

Required for tests: `numpy`, `h5py`, `Pillow` (and optionally others in `requirements.txt`).
